"""Synthetic dataset generator for LeakSentry — PLACEHOLDER (build step 2).

Will generate a realistic, internally-inconsistent dataset for fictional B2B SaaS
"Northwind Cloud" (~150 customers, ~400 contract lines, ~2000 invoices, ~12 months
usage), inject 5 leak archetypes + noise, and write a labeled `ground_truth.csv`.

Run: `python data/generate_dataset.py`
"""

if __name__ == "__main__":
    raise SystemExit(
        "Not implemented yet — generate_dataset.py is wired up in build step 2."
    )
